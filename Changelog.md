# Changelog

## v0.1.10

### 升级须知

- 292 模板有效期恢复为 1 小时（默认 3600 秒，自动提前续采）。v0.1.6–v0.1.9 默认的 240 秒 / 提前 2 分钟会在升级后自动改回，自定义过的值保留。
- 240 秒改为 Cookie 的有效期。Cookie 不再随模板保存，改为每个账号一份、只保存在内存中；升级后状态文件里的旧 Cookie 会被清除。
- 开启“要求有效 Turn State”时，所选账号请求不在 State 模型范围内的模型，不再要求模板，也不经过 State；请求所选模型时仍需要有效模板。

### 后端

- 所选账号的每次上游响应（业务响应和探测，包括 312 和错误响应）都会刷新该账号的 Cookie。Cookie 在有效期（默认 240 秒）内，所选模型的每个请求都会带上，与是否注入模板无关。
- 模型列表中的第一个模型采到 292 后，默认 30 秒后对该桶再采一次，持续刷新 Cookie；刷新失败则停止，等模板正常续采。间隔设为 0 可关闭。
- 判断请求是否属于 State 范围时，与 CPA 一样忽略大小写、任意括号后缀和账号前缀，所选模型的变体写法不会绕过有效 State 要求；客户端使用模型别名时，调度优先选择不会被拒绝的账号。
- 修复：Key 绑定唯一分组时，在 API Key 列表里清空这个 Key 的单独规则后，所有账号都被拒绝。现在清空后按分组授权访问，与确认提示一致；只设置了模型或拒绝项的单独规则仍照常限制。
- 并发探测数和狂暴续采的设置错误提示补充中文。

### 前端

- State 设置新增“Cookie 有效期”和“采到 292 后再采间隔”；桶就绪度的账号列显示 Cookie 数量和剩余秒数，过期显示“已过期”，模板显示下次刷新 Cookie 的倒计时。
- 模型测试的“自定义问题”示例改为让模型自我介绍并说明是哪个模型，移除旧的示例题。
- 更新“要求有效 Turn State”开关和 Cookie 注入的说明。

## v0.1.9

### 后端

- 并发探测不再同时探测同一个采集桶，避免一次续采重复消耗探测次数；只有处于狂暴续采窗口的桶才会同时多路探测。
- 上游返回降级长度的 State 时，即使严格探测也失败，采集日志也会写明是降级 State；响应格式不符时显示实际的 Content-Type。

## v0.1.8

### 升级须知

- 服务器采集默认同时进行 3 个探测，可在 State 设置里改为 1–10。
- 不在 State 采集范围内的账号和模型，业务请求不再经过 State 处理，也不会再从这些账号的响应里学习模板。

### 后端

- 新增狂暴续采（默认关闭）：所选模板进入最后 N 分钟（默认 1 分钟）仍未续采成功时，最多同时发起 10 个探测，各走不同出口。
- 新增“突破 API Key 分组池，动态路由重试”（默认关闭）：请求在 Key 所属分组里已无可用账号时，改用其他持有同模型有效模板的所选账号。
- 续采时优先使用当初采集成功的出口。
- 未开启“要求有效 Turn State”时，调度也会优先避开采集桶已过期的所选账号，分组里没有其他账号时才照常使用。

### 前端

- State 设置新增并发探测数、狂暴续采和分组外重试选项。
- 采集桶就绪度中不会被注入的桶置灰显示。

## v0.1.7

### 后端

- Turn State 采集遇到上游对出口返回的 HTTP 403（Cloudflare 人机验证、HTML 拦截页或不支持的国家/地区）时，不再暂停账号，下一次探测直接换用其他出口；其他 403 仍按账号问题暂停。

### 前端

- 保存 State 设置后停留在原来的视图（运行状态、采集范围、代理出口池、策略设置）和滚动位置；外层页面在保存后刷新时也会恢复。

## v0.1.6

### 升级须知

- Turn State 模板有效期仍为默认的 3600 秒时，升级后自动改为 240 秒，并在到期前 2 分钟续采；自行设置过的有效期保持不变。
- 开启 Cookie 注入后保存 State 设置，会给所选 Codex 账号的认证文件加上 `Cookie: $Cookie` 转发头。

### 后端

- 新装默认的 Turn State 模板有效期为 240 秒，到期前 2 分钟续采。
- 采集和从响应学习模板时，会一并保存上游下发的 Cookie；注入模板时同时注入这些 Cookie，同名的客户端 Cookie 会被替换。可在 State 设置里关闭。

### 前端

- State 设置新增“注入采集到的 Cookie”开关，模板表显示每个模板保存的 Cookie 数量。

## v0.1.5

### 后端

- 流量抓包的账号列表不再显示未提供邮箱的认证文件账号，也不能再监听这类账号。

### 前端

- 流量抓包的请求、发往上游的请求体和响应可以分别收起，展开状态保存在当前浏览器。
- 已捕获的请求可以按账号查看；只看单个账号时，表格不再显示账号列。
- 上游响应声明的模型与请求的模型不一致时，以橙色标出。
- 流量抓包、模型测试、Codex Turn State、Team 账号并发以及分组和路由的账号选择中，已选项在打开列表、搜索或切换筛选时置顶显示。

## v0.1.4

### 后端

- 分组只选了账号、没有列出模型时，这些账号可用于任意模型，不再被其他分组列出的模型白名单限制。例如 Key 同时绑定只列 gpt 模型的唯一组和只选 xai 账号的通用组，现在也能请求 grok，且只会使用通用组的 xai 账号。
- 被拒绝的模型、Key 自己的模型规则和只列模型不选账号的分组仍会照常限制可用模型。

## v0.1.3

### 升级须知

- 模型测试的预制题只保留糖果保证题、鹈鹕骑自行车 SVG 动画题和自定义提问，原有的 JSON、约束推理、代码审查、布尔表达式、交换追踪题已移除；Codex Turn State 的质量检查弹窗同步只提供前两道题。
- 流量抓包切换「注册响应钩子」后，页面会把本插件配置原样保存一次，让 CPA 立即重新注册插件，不再需要手动重启。

### 后端

- 模型测试同时进行的上限从 4 个提高到 8 个；账号列表返回认证文件单独配置的代理（隐藏用户名和密码）。
- 糖果题改用完整原题，自动检查回答最后一行的最终答案是否为 21；只在推理中提到 21 不算通过。
- 流量抓包支持同时监听多个账号并逐个暂停，记录所属账号和上游响应中声明的模型。

### 前端

- 模型测试页改为上下布局：测试设置、测试账号、回答对比依次排列。
- 支持并发执行和连续测试：可设置每个账号测试次数（1–100）和并发请求数（1–8），同一账号依次执行。
- 回答对比按账号汇总连续结果、检查通过数、声明模型及次数和平均耗时，下方逐次结果表可筛选并展开完整回答。
- 单独挂了代理的账号在列表中显示代理标签，并提示其他账号走全局代理还是直连。
- 鹈鹕题的 SVG 动画可直接播放并重新播放，脚本、样式和外部资源会被过滤。
- 流量抓包：账号改为搜索加勾选列表，列表显示账号和上游响应模型，正文默认打码，请求、响应和完整记录可下载为文本。

## v0.1.2

### 升级须知

- 新增「流量抓包」页面，默认不监听，不影响现有请求；只抓请求时不注册额外钩子。
- 如需同时看到响应内容，在该页勾选「注册响应钩子以捕获响应」后重启 CPA 或重新加载插件配置；开关保存在 `<state_file>.traffic-capture.json`。Codex Turn State 已开启时响应钩子本来就已注册。

### 后端

- 新增流量抓包：选定一个上游账号后，记录分配给它的请求路径、请求头、请求体、协议转换后发往上游的请求体，以及响应头和响应正文（流式响应按分块追加），重试到其他账号的请求会单独标注。
- 抓包只保存在内存中，最多 100 条、每个正文 2 MiB、总计 48 MiB；可能携带密钥、令牌、Cookie、密码的请求头值会打码。监听由页面轮询续期，离开页面约 45 秒后自动停止，插件不运行后台定时器。

### 前端

- 新增「流量抓包」标签页：选择账号开始监听，实时刷新请求列表，查看请求与响应全文，支持 JSON 格式化和一键复制。

## v0.1.1

### 升级须知

- 插件商店源（registry.json）的版本号同步为 0.1.1，此前停留在 0.0.13；通过自定义商店源安装的实例可正常识别新版本并提示更新。
- 服务器采集器部署说明中的源码标签和 `CPA_COLLECTOR_VERSION` 示例同步为 v0.1.1，按文档构建的采集器与插件版本一致。
- 功能与 v0.1.0 相同，无需迁移。

## v0.1.0

### 升级须知

- State 新增全局总开关，升级后默认开启，行为与之前一致。关闭后强制 Astra、注入、响应学习、有效 State 门槛、服务器采集和手动探测全部停止，采集账号的业务请求原样直通；各项设置、模板和冷却保留，重新开启即恢复。
- 在 State 关闭状态下重启 CPA 或重新加载插件配置，插件不再注册模型路由、响应拦截和流式分片拦截，业务请求没有 State 额外开销；之后重新开启时，强制 Astra 和响应学习需再次重启或重载后才恢复，注入与门槛立即生效。
- 总开关关闭后服务器采集器不再发送探测，但仍会心跳；如需完全停止，请停止 cpa-state-collector 服务。

### 后端

- 新增 State 全局开关，随 State 设置持久保存；关闭时请求、响应和路由回调在解析负载前直接返回，调度不再过滤采集账号，探测、自检和开始采集会被拒绝。
- State 状态接口返回回调注册情况，便于判断是否需要重载插件。

### 前端

- State 页面重构为「运行状态」「采集范围」「代理池」「策略设置」四个视图，关键数据和采集操作集中在顶部，说明文字默认折叠；保存改为底部统一保存栏，仅在有未保存修改时出现，保存校验会自动切换到出错字段所在视图。
- State 注入改为「关闭 / 仅观察 / 注入」三选一，新增全局开关及其状态提示；新配置时自动展开设置向导。
- 模型测试页面重构：模型、提示词预设和运行按钮集中在顶部操作栏，提示词默认折叠（自由问答自动展开），说明文字默认折叠；默认模型为 gpt-6-astra。

## v0.0.14

### 升级须知

- 强制 Astra 需要 CLIProxyAPI v7.3.4 兼容的提供商模型路由能力，默认关闭；在 State 页开启并保存。需要同时注入 State 时，请为所用账号采集 Astra 模板。

### 后端

- 支持将显式 GPT/Codex 文本模型请求路由到 gpt-6-astra，保留推理强度，按目标模型选择 State 模板，重启后保留开关设置。
- 自定义别名、其他提供商、图片/音频请求及内部采集、质量测试保持原路由；关闭开关恢复宿主原有路由。

### 前端

- State 页面新增「强制 Astra」开关、中英文说明及保存入口，独立于 State 注入和观察模式。

## v0.0.13

### 升级须知

- 更新插件后重启 CPA；原有独立采集器可继续使用，本版未开启并发采集。
- 代理连通性改为检测网关 TCP 端口，不再访问 OpenAI；显示的网关 IP 不代表代理出口，连通不保证代理认证或 State 采集成功。
- 响应 State 观测仅保留在内存，重启清空。丢弃模板需先停采，缺票可能使受有效 State 门槛保护的桶暂不可用。
- 丢弃记录随 State 主文件和 `.runtime.json` 持久保存，备份须一并保留；旧版不识别丢弃记录，回退后同一旧票可能重新被学习。

### 后端

- 增加精确账号／模型的响应 State 观测，区分实际注入、未注入及响应头长度，保留近 24 小时汇总和最近事件；修复重试时旧账号与注入标记残留。
- 支持按完整指纹丢弃指定模板，拒绝过时页面误删；写入成功才移除，阻止同票在原签发时间满一小时前重新学习、采集或恢复，保留并发续采的新票。
- 代理入口检测使用 5 秒 TCP 连接，连接后立即释放，不发送凭据或查询出口 IP；分别报告解析失败、超时和连接失败。
- 允许停用账号执行管理员模型诊断，保留身份核验、并发与 State 保护，不修改账号启用状态；兼容仅提供 Provider 的认证文件分类。

### 前端

- State 账号模块新增「一键检测降智」，支持多账号、多预置题及停用账号，最多 50 次串行请求；逐题显示通过、未通过、请求失败或人工评估，支持停止与关闭收尾。
- 模型测试隐藏不支持协议的条目，整张卡片可勾选；准备测试后退出登录会清理未发送的临时名额，防止请求串到其他账号。
- 账号页过滤未知提供方条目，缺邮箱的真实账号显示认证索引；新增筛选全选及批量设置 0–1000 并发，提示隐藏已选项，修复保存后旧查询覆盖显示的问题。
- State 页面新增响应观测和精确模板丢弃入口；连通性分别统计连通、失败及未确认，手动移除失败项仍需保存。
- README 记录 arden 主分支 `d3efa48` 与 NanSsye r18 `8cab81c` 的功能对齐范围，并补充测试、备份与操作指引。

## v0.0.12

### 升级须知

- State 默认行为保持不变：静态出口冷却 55 分钟、轮换窗口 10 分钟、账号暂停 10 分钟、轮换池每窗口最多 10 次。现有独立采集器可继续使用。
- 需要持续轮询代理时，在「冷却与重试」点击「取消冷却与次数限制」并保存，将四项及每小时探测上限设为 0。持续尝试会消耗额度与代理流量，不能解除上游限制。

### 后端

- 支持分别配置静态出口、轮换池窗口、账号暂停及轮换尝试次数；设为 0 可关闭对应失败等待，轮换窗口或次数任一为 0 时不限该池重试次数。
- 保存后采集器在下一心跳应用设置；关闭限制可跳过已有对应失败冷却，正数设置保留现有截止时间及已用次数。继续保留串行请求、2 秒间隔、有效模板续采及已配置的小时预算。
- 采集日志显示实际暂停时长及本次轮换尝试上限，区分不限次数与旧记录；重启保留配置、请求间隔和有效窗口内的尝试计数。

### 前端

- State 页面新增「冷却与重试」设置和取消限制按钮，支持时长 0–1440 分钟、次数 0–100；快捷操作只修改草稿，保存后生效，不自动启动采集。
- 更新中英文页面指引与 README，说明持续轮询、已有冷却、正常续采、小时预算和自动丢弃代理之间的关系。

## v0.0.11

### 升级须知

- State 采集日志和实时进度改为显示完整代理 URL（含用户名和密码），仅管理员可见；分享日志前请自行隐藏代理凭证。
- 严格探测默认关闭，每小时采集上限默认 0（不限）；开启后可能增加探测耗时或续采等待。v0.0.10 的独立采集器可继续使用，自动采集仍由服务器进程驱动。
- 新出现的 HTTP 401/403 账号暂停由 55 分钟调整为 10 分钟；已保存的旧暂停按原期限结束，必要时可手动清除一次。清除不会重置小时预算或解除上游限制。

### 后端

- 采集按两个代理池的统一顺序循环，每次尝试后前进一条，成功后下一个桶继续使用下一条；重启、编辑及自动丢弃代理后保留下一可用位置。
- 增加可选严格探测，在 25 秒与 1 MiB 范围内确认上游响应完成，再保存有效 State；校验失败保留尚未过期的旧模板，不误删代理。
- 增加跨账号、模型及代理池共享的滚动小时采集预算，手动与自动采集统一计数，重启或清除冷却不重置；耗尽后保持任务开启并等待恢复。
- 修复清除冷却后仍等待旧调度时间的问题；在线采集器在下一心跳重新检查，停止的任务保持停止，日志区分账号暂停和出口冷却并显示实际检查时间。
- 生图、改图接口跳过聊天 State 门槛、注入与学习，继续执行分组、模型、额度及并发控制；不改变 CPA 对图片接口的支持范围与超时。
- 模型测试返回填写模型、解析别名后的发送模型及上游声明模型，标记缺失、不一致与冲突，仅用于诊断，不改变计费或路由。

### 前端

- 模型测试改为左侧账号栏，支持按服务商筛选、搜索及多选，最多 50 个账号顺序批量测试；显示逐项结果，失败后继续下一项，停止或离开页面不再发送待执行项。
- 增加鹈鹕骑自行车 SVG、糖果保证、布尔表达式及交换追踪预设，展示题目来源和验收标准；SVG 仅作受限静态预览，测试结果不作为模型身份或整体能力证明。
- State 页面增加严格探测开关、小时预算与恢复提示，日志显示 HTTP 状态及下次检查时间，补充采集、冷却和相关项目指引。
- API Key 分组的号池属性移至设置最下方；默认继续允许未分组 Key 使用可访问模型，只有主动开启拒绝未分组选项时才拦截。

## v0.0.10

### 升级须知

- 自动采集改由独立的 `cpa-state-collector` 常驻进程调度；更新插件后还需在服务器安装本次发布的采集器，按部署说明配置 systemd 或 Docker。只更新插件、未安装采集器时不会自动探测；v0.0.9 不支持新采集器接口。
- 计费数据库升级到 v20，保留历史数据，已有分组默认为普通。升级前停止 CPA 并备份；旧版不能直接打开升级后的数据库，回退需恢复升级前备份。
- 备份需同时包含原 `.turn-state.json`、新增 `.turn-state.json.runtime.json` 与 `.turn-state-runner.json`，分别保存采集配置、最新模板与冷却、服务器启停状态。
- 保存或开始 State 采集会关闭所选账号的上游 WebSocket，客户端可继续通过 CPA 的 HTTP/SSE 桥接使用 WebSocket；旧会话可能需重连一次，移出采集范围不会自动重新开启上游 WebSocket。
- 业务响应学习先更新内存，由管理同步或采集器落盘；成功持久化前重启可能丢失新学习的缓存。主动探测成功仍立即保存。

### 后端

- 增加服务器采集器、持久启停、单实例租约和最近采集日志；关闭网页不再停止采集，网络故障自动退避，重启后恢复开启状态。停止后只允许当前一次请求收尾，自动采集与手动探测、自测互斥。
- 修复前面的失败桶长期占用探测机会；按桶公平轮转并优先续采，轮换池按账号与模型共用失败预算，成功后按续采时间重新开启预算。上游拒绝后的账号冷却不因保存失败而丢失。
- 提前续采期间继续使用未过期的旧模板，新模板保存成功后原子切换；失败或重复令牌不清空有效模板，也不延长旧模板寿命。
- State 磁盘保存移出业务共用锁，探测不再反复重写完整代理池；新宿主省略流式回调中未使用的历史片段，减少响应路径开销。
- API Key 分组支持普通、唯一和通用属性；唯一组只与该 Key 已绑定的通用组合并，排除普通组，保留直接权限约束与拒绝项优先。多个启用唯一组冲突时拒绝修改。

### 前端

- State 页面统一控制服务器采集，展示在线、等待、探测和停止收尾状态；刷新、切页、退出登录或关闭浏览器不改变服务器启停意图。
- 模板表格分别显示剩余有效时间和距离续采窗口，标注续采中旧模板仍可用；补充安装、升级、备份及续采衔接指引。
- 分组号池改为紧凑账号卡片，移除额度查询，增加 CPA 账号启停开关；停用对全部绑定分组的新请求生效，无法安全切换的账号明确提示。

## v0.0.9

### 升级须知

- 自动丢弃异常、不合格代理默认关闭；开启后按实际探测结果立即删除并保存对应代理 URL，默认两池合计至少保留 10 条，可自行调整。
- 连续采集由浏览器驱动：刷新可恢复、临时故障自动重试；关闭页面或离开采集页时暂停，返回后恢复。管理登录过期后需重新登录，明确停止或退出登录才会清除任务。
- 模型测试使用 CPA 7.3.4 兼容宿主的显式代理诊断接口，消耗上游账号额度，但不生成下游 API Key 账单；账号代理配置错误或连接失败时拒绝请求，不自动改走直连。

### 后端

- 增加代理自动淘汰及最少保留数量，按完整 URL 和所在池精确删除；不因账号认证、权限或额度错误误删，不会自动删空后改走直连。
- 自动删除与配置保存使用同一持久化流程，写入失败保留原代理；状态返回配置版本，避免删除后的界面过期或旧草稿覆盖新代理池。
- 模型测试支持指定账号与模型，沿用账号并发、风控和有效桶保护，提供有期限的单次测试；响应只用于展示与测试断言，不重建计费数据。

### 前端

- 修复探测错误日志刷新后显示 `[object Object]`；临时服务、网络和状态读取错误改为退避重试，保存配置和重新登录后恢复已有任务。
- 代理连通性检测改为最多 10 并发，单条检测异常不终止整批，停止后等待已发出的检测结束。
- 账号、模型和动态整组规则恢复整行点击，在允许、拒绝、未选择之间循环；动态整组规则及绑定 API Key 默认展开，修复窄屏工具栏溢出。
- State 页面增加使用步骤与常见结果说明，明确代理检测、采集成功、注入模式和有效桶保护的关系；GitHub 链接增加图标。
- 新增模型测试页，提供指令遵循、逻辑推理、代码题与自由问答预设，支持编辑提示词和比较最近回答；生成代码只展示，不执行。
- 重写中英文 README，明确独立 fork、第三方插件源的真实添加入口、安装启用、旧 ID 迁移和数据备份步骤。

## v0.0.8

### 升级须知

- 插件 ID 改为 `cpa-team-manager`，配置键、动态库和访问地址同步调整。升级前停止 CPA、备份数据并移出旧库；更名配置键时保留原 `state_file` 路径，以继续使用已有分组、订阅和桶。
- 加入采集列表的账号默认要求有效桶才能处理业务；桶过期、注入关闭或无法替换时拒绝请求。此保护要求 CPA 7.3.4 或含相同修复的宿主，仅支持 HTTP/SSE，受保护账号的 WebSocket 请求会被拒绝。
- 账号并发与风控设置分别保存在数据库旁的 `.account-runtime.json`、`.risk-control.json`；接入凭证保存在 CPA 认证目录，请一并持久化和备份。

### 后端

- 接入 OpenCode Go / Zen、CommandCode 和 Cline Pass，支持 Cline 设备登录与手动刷新，查询可取得的服务端订阅额度。
- OpenCode 按已验证协议发布多类上游通道，明确排除宿主不支持的 Google 路径和未知协议模型；额度只关联准确确认的凭证。
- 替换密钥或刷新 Cline 凭证时分阶段迁移准确的分组、路由与 Key 绑定，新旧凭证共用并发上限；中断后可修复通道并重试完成。
- 增加逐账号用量汇总与并发限制，正确释放流式、断开及账号切换后的并发槽；无法确认的历史用量明确标注。
- 对采集账号在调度和最终发往上游前检查有效桶，防止过期、缺失模板的账号混入业务请求。
- 增加本地关键词风控、模型范围、观察与请求前拦截模式，支持哈希记忆及可清除的事件记录。

### 前端

- 新增账号接入、账号管理与风控中心；分组内账号以标签展示实际返回的 5 小时和周额度，缺失窗口不显示。
- Turn State 各设置区块提供统一保存入口与未保存提示，保存失败保留草稿；独立展示业务保护开关与生效范围。
- 桶就绪度显示代理 IP/主机与端口，保留认证信息隐藏；采集表格、剩余分钟、提前续采、代理检测和日志进度继续可用。
- 顶部增加项目 GitHub 入口；持久化检查仅在设置页出现且只显示需要处理的风险。

## v0.0.7

### 升级须知

- 计费数据库升级到 v19，保留已有订阅、余额和历史记录；升级前请备份数据库及旁边的 `.turn-state.json`，回退旧版时需恢复升级前备份。
- 含模型范围额度池的订阅计划需指定具体请求模型；动态 `auto` 无法稳定归属额度时返回 `503 quota_model_unresolved`。仅设置全部模型额度的计划不受影响。
- 默认探测模型改为 `gpt-6-astra`、`gpt-5.6-sol`，自动纠正与旧默认值完全一致的列表，保留自定义或清空的列表。

### 后端

- 订阅支持按单模型、多模型或模型类别配置独立额度池，每个 API Key 分别累计；同一请求命中的全部额度同时生效，局部耗尽不影响其他模型。
- 增加探测结果和请求决策细分计数，保留最近探测结果；支持指定桶经 CPA 自检，以及确认后清除失败冷却，保留有效模板和正常续采等待。
- 检查可识别容器部署中的插件库、数据库及 State 文件挂载，提示容器层、内存文件和缺失插件库风险；无法判断时不作持久化保证。
- 修复探测完成与重新配置之间的统计竞态。

### 前端

- 软件更名为「API Key 团队管理」，更新插件商店名称、简介及中英文搜索词；订阅计划改为独立标签页，支持配置和查看各模型范围的额度。
- 重构上游凭证选择弹窗，分开路由、凭证与模型，增加来源、状态和已选筛选；显式设置允许、拒绝，筛选保留选择，批量操作只影响当前匹配项。
- 重构 Turn State 页面，集中展示探测控制台、桶就绪度、决策计数和逐桶操作；优化账号搜索与批量选择，代理列表保持展开。
- 探测刷新后恢复任务与最近日志，同一管理身份的多个标签页协调运行；关闭页面后暂停，明确停止后不再自动恢复。
- 清除冷却和单桶请求增加影响说明与二次确认；持久化风险在管理页显著提示。

## v0.0.6

### 升级须知

- 替换插件动态库后请重启 CPA；已有账号、代理、模板与默认提前续采策略保留。旧版已保存的冷却先按原期限结束一次。
- 采集页现在完整显示已保存代理；把已加载的文本框删空并保存会清空对应池。删除无效代理先修改草稿，保存后才生效。

### 后端

- 提前续采支持自定义分钟数，0 保留自动策略；调整后重新安排成功出口续采，保留失败出口冷却。
- 增加代理连通性检测和出口 IP 采样，区分连接失败、认证失败与上游拒绝；403、429 等不确定结果不会被批量删除。
- 管理员可分页读取完整代理配置，保留大配置分块保存，并校验编辑版本以避免覆盖其他页面的修改。
- 提供实际采集代理的实时进度，保存成功模板的脱敏代理地址和来源。

### 前端

- 采集结果改为账号与模型矩阵，显示剩余有效分钟数、已就绪桶数和即将续采状态。
- 采集账号与代理默认展开，已保存代理直接显示在文本框，刷新和切换页面保留未保存内容。
- 支持逐池测试连通性、查看进度和删除无效代理；读取或保存冲突时可重新加载已保存列表。
- 采集日志显示成功代理地址、当前代理序号与总数，轮换池显示重试次数；停止后正确显示当前请求的最终结果。

## v0.0.5

### 升级须知

- 替换插件动态库后请重启 CPA；主动探测不再依赖服务器安装 curl，已有账号、代理和模板配置继续保留。
- 每个代理池保存超过 2,000 条后，旧版 v0.0.4 无法读取该配置；回退前请缩减代理池或恢复对应备份。
- Turn State 状态接口不再返回代理地址列表，请使用 `proxy_counts` 读取静态池和轮换池数量。

### 后端

- 探测改用 Go 内置 HTTP 客户端，支持 HTTP、HTTPS、SOCKS5、SOCKS5H 及代理认证；每次新建连接，总超时 25 秒，保留原代理重试规则。
- 支持显式探测已停用的 Codex OAuth 账号，采集不改变账号启用状态，业务调度仍排除停用账号。
- 大配置支持分块上传、完整校验后一次性保存；上传中断或配置冲突不会覆盖现有设置。静态池、轮换池各支持 20,000 条，完整配置上限 16 MiB。
- 新安装默认探测模型为 `gpt6`、`gpt-5.6-sol`，保留已有自定义或清空的模型列表。
- 管理接口提供可翻译的错误和状态提示，修复分析中真实名称与“未知模型／来源”的统计分组混淆。

### 前端

- 新增简体中文、英文切换，支持跟随宿主语言并保留独立页面的语言选择。
- API Key 分组、Codex Turn State 改为独立标签页，切换页面或语言时保留未保存内容，离开采集页面后停止后续探测。
- 保存大量代理时自动分块提交，避免单次请求过大触发常见网关的 413；失败时保留输入内容供重试。
- 停用账号可勾选采集并标注状态；未分组拦截开关统一显示为“拒绝所有未分组的apikey”。
- 增加 Turn State 原项目出处链接及英文使用说明。

## v0.0.4

### 升级须知

- 计费数据库格式升级到 v18，首次启动自动迁移，已有分组默认启用；升级前请备份计费数据库，升级后请勿直接回退旧版。
- 移除 X-Forwarded-For 拦截及其设置入口，旧配置不再生效。
- 集成的 Codex turn-state 默认关闭，与独立插件选择其一启用；HTTP/SSE 注入需要 CPA 7.3.4 或包含相同请求头转发修复的版本。
- 主动探测需要服务器安装 curl，会消耗上游额度；连续探测由管理页面驱动，关闭页面后停止发起新探测。备份时请一并保存计费数据库旁的 `.turn-state.json` 文件。

### 后端

- API Key 分组支持启用、停用；同一 Key 合并所有启用分组的授权，黑名单优先，停用账号不阻挡其他分组的可用账号。
- 按宿主最新候选状态调度，账号重新启用后恢复参与选择，无可用且获授权的账号时拒绝请求。
- 集成 turn-state 响应学习、按账号和模型隔离的模板注入，以及静态、轮换代理探测；支持 `replace-only`、`always`、观察模式和有效期配置。
- 探测跳过停用或已删除的账号，按模板实际到期时间续采；保存配置、清空模板与探测串行执行，避免旧结果回写。
- 安装脚本改为从本仓库下载发布包，发布包附带第三方许可证说明。

### 前端

- 分组卡片增加启用开关，修复上游凭证分类、筛选和批量选择，显示账号停用及暂不可用状态。
- 设置页增加 turn-state 配置、模板状态、脱敏代理池和连续探测入口，并保留未保存的编辑内容。
- 停用或已删除的探测账号保留为可移除选项；配置刷新、保存与清理按顺序执行，避免并发操作覆盖新状态。

## v0.0.3

### 升级须知

- 计费数据库格式升级到 v17，首次启动自动迁移，已有分组的直接选择为空；升级后请勿直接回退旧版，升级前请备份计费数据库。

### 后端

- 分组可直接选择上游凭证和模型（白名单、黑名单），无需先创建路由规则；与分组路由、Key 路由及 Key 直接选择合并生效，黑名单优先。
- 既未绑定路由也未直接选择的分组视为空组，Key 所属分组全部为空组时拒绝访问。
- 保存分组时校验新增的上游凭证是否仍然存在。

### 前端

- 分组编辑改用与 Key 路由相同的选择器，支持按类别和搜索选择凭证与模型，以及批量白选、黑选。
- 分组卡片显示直接选择的上游凭证和模型。

## v0.0.2

### 升级须知

- 计费数据库格式升级到 v16，首次启动自动迁移并保留现有数据；升级后请勿直接回退旧版，升级前请备份计费数据库。
- X-Forwarded-For 拦截默认关闭；若 CPA 位于会添加 X-Forwarded-For 的反向代理、负载均衡或 CDN 之后，开启后所有命中关键词的模型请求都会被拒绝。

### 后端

- 新增 X-Forwarded-For 拦截：模型名称包含任一关键词（不区分大小写）且请求携带 X-Forwarded-For 时，返回 403 和自定义提示语，保存后立即生效，不受访问控制开关影响。
- 拦截写入插件日志，同一 Key 和关键词每 10 分钟最多记录一条，不记录请求头中的 IP。

### 前端

- 设置页增加 X-Forwarded-For 拦截开关、模型关键词和提示语配置。
- 修复保存过程中重新登录后，访问控制保存按钮一直显示“正在保存…”的问题。

## v0.0.1

### 升级须知

- 本分支版本从 v0.0.1 开始，原版发布记录保留在下方。升级前请备份计费数据库。
- 已配置路由或分组的 Key 仅能使用白名单凭证；空白名单禁止访问，原仅限制模型或仅设置黑名单的规则需补选允许凭证。
- 访问控制默认开启，默认拒绝未分组 Key 默认关闭；开启后，未分组的新旧 Key 均禁止访问。

### 后端

- 支持 Key 加入多个分组、按组绑定路由及批量管理成员，分组与开关保存后重启仍然生效。
- 多组凭证白名单合并，黑名单优先；清空直接凭证选择仍禁止访问，并核对最终选中的上游凭证。
- 未定价模型正常访问并记录用量，费用为零，Token 与请求次数额度仍然生效。
- API Key 最多显示首尾各 8 个字符，短 Key 按长度脱敏，4 个字符仅显示首尾各 1 个字符。
- 提供本仓库的 CPA 插件商店注册信息及本地构建说明。

### 前端

- 增加分组管理、Key 多组选择、批量分组和访问控制开关。
- 上游凭证增加 Codex、xAI、OAuth 分类标签及搜索，Key 列表与分组成员支持搜索和分组筛选。
- 优化窄屏凭证选择与分组编辑，保存按钮保持可见，保存失败时保留编辑内容并提示错误。

## v1.3.11

### 前端

- 管理员可在认证文件页消耗主动重置次数，确认后立即重置 Codex 限额。
- 统一标签、下拉选项与提示中的分隔符间距，显示更紧凑。
- 修复 Safari 中路由规则提示框高度异常的问题。

## v1.3.10

### 升级须知

- 已有订阅计划保持原独立周期；启用统一周期后请勿直接回退旧版。

### 后端

- 订阅计划支持统一周期，各额度窗口可分别指定下次开始时间，所有绑定 Key 按相同时间重置，各 Key 额度独立计算。
- 手动重置额度不改变统一周期的重置时间。

### 前端

- 新建和编辑订阅计划可选择周期模式，按重置周期预填下次开始时间，并提示无效输入。
- 优化时间选择器和窄屏布局，订阅计划卡片显示下次重置时间。

## v1.3.9

### 升级须知

- 兼容已有路由规则；使用黑名单后，旧版本无法执行这些限制，请勿直接回退。

### 后端

- 路由规则支持模型、整类凭证和单个凭证黑名单，黑名单优先于白名单。

### 前端

- 模型和凭证支持未选、白名单、黑名单三态切换，并提供全白选、全黑选和清空操作。
- 统一路由编辑列表、选框与类型图标，支持折叠和筛选，优化窄屏布局。
- API Key 路由规则以胶囊展示，支持悬浮查看完整列表、弹窗编辑和清空确认。

## v1.3.8

### 后端

- 优化大数据量下的分析、请求事件、错误事件和插件日志查询，加快 Key 筛选与条数统计。

### 前端

- 加快 API Key 与设置页加载，减少重复请求；修复退出登录后仍继续查询的问题。
- 统一页面留白与卡片高度，优化窄屏下时间范围、路由选择和编辑弹窗的显示。
- 页面底部新增版权、版本信息及仓库和许可证链接。

## v1.3.7

### 升级须知

- 升级前请备份数据；升级后保留历史记录，回退旧版时需恢复升级前的备份。

### 后端

- 修复 API Key 直接指定模型时绕过已绑定路由凭证限制的问题。
- 去掉凭证表，凭证信息直接记录到请求事件表中。
- 增加配置型 AI 供应商表，持久化管理员前端 sync 到后端的配置型 AI 供应商列表。

### 前端

- 取消自动刷新，修复路由模型和凭证选项加载不完整的问题。
- 修复筛选条件、时间范围和图表选项记忆异常的问题。

## v1.3.6

### 后端

- 订阅计划支持金额、Token、请求数三种额度，可按窗口单独设置或组合使用。

### 前端

- 统一订阅计划与路由规则卡片样式，优化计划编辑、额度展示及并发限制布局。
- 修复 API Key 登录未勾选“记住密钥”时，刷新页面退出登录的问题。
- 修复确认弹窗错误触发浏览器自动填充的问题，精简重置提示。

## v1.3.5

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 后端

- 未定价请求按 0 成本参与分析汇总，不再隐藏总成本和趋势。
- `codex-auto-review` 内置价由免费调整为 models.dev 的 GPT-5.4 价格，包含缓存读和长上下文档位。

### 前端

- 请求事件 CSV 导出时，未知 Token 数留空，不再显示为 0。

## v1.3.4

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 后端

- 新增 `codex_fast_mode_billing` 开关，默认关闭；开启后，Codex 请求包含 `service_tier=priority` 时按 2.5 倍计费。
- 请求记录保留计费倍率，关闭开关后历史费用与倍率标记保持不变。

### 前端

- 请求事件合并显示上下文档位和倍率，例如“标准 · x2.5”；成本明细注明单价和金额已包含倍率。
- 请求事件 CSV 导出新增 `multiplier` 列，区分普通计费和 2.5 倍计费。

## v1.3.3

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 后端

- 按资源拆分查询接口，配置修改可直接返回最新视图，减少后续查询。
- 请求事件和错误事件提供稳定 ID 与分页快照，避免滚动加载和导出时重复、遗漏记录。

### 前端

- 统一按需加载、共享缓存和增量渲染，减少重复请求与界面重建；仅自动刷新当前页面，分析范围超过 24 小时时不自动刷新。
- 配置提交成功后立即更新界面；提交期间锁定表单，失败时保留编辑内容。
- 请求事件统一显示成功／失败和成本，精简成本明细，并优化事件及插件日志卡片高度。
- 请求事件和错误事件支持按筛选条件导出 CSV，仅保留展示字段，使用英文变量名和未格式化的值。
- 支持一键导入 CPAMP 的 API Key 备注，跳过未变化和无法匹配的记录。

## v1.3.2

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 后端

- 新增 `codex-auto-review` 和 `gpt-image-1.5` 内置价格，优先级低于自定义价、高于参考价。
- 新增 debug 日志开关，默认关闭；info 和 error 日志不受影响。

### 前端

- 分析图表刷新时复用已有图表实例，减少闪烁。
- 请求事件在缓存写为零时隐藏缓存写列。

## v1.3.1

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 前端

- 分析卡片新增按日／小时平均请求数、Token 和成本，以及缓存率峰值。
- 修复插件日志级别计数，支持滚动自动加载，并固定列表高度、显示加载进度。
- API Key 表格的订阅计划下拉框按选项内容调整宽度，避免过宽。

## v1.3.0

### 升级须知

- 升级前请备份数据。支持迁移 v1.0.0 至 v1.2.3 的数据库；v0.8.4 及更早版本需使用新数据文件。
- 已有定价保留为自定义价，删除后可使用参考价；原通配定价需改为按模型 ID 配置。
- 原订阅计划迁移为单个额度窗口，保留已用额度；“不重置”计划改为 365 天周期。

### 后端

- 重构模型价格体系：自定义价优先、models.dev 参考价补充，无可用价格时拦截请求；参考价维护不依赖前端访问。
- 重构 API Key 管理体系：首次上报用量时自动纳入管理，删除后保留历史关联，重新添加后沿用配置与额度。
- 重构订阅计划体系：支持多个自定义额度窗口，从首次使用开始独立计算周期，任一窗口用尽即暂停新请求。
- 请求事件及错误事件保留期限延长至 365 天。

### 前端

- 支持自定义价删除、已删除 Key 管理和多窗口额度展示。
- 用量分布新增请求数、成本占比；时间范围选择器支持按小时、按天及自定义区间选择。

## v1.2.3

### 升级须知

- 从 v1.0.0 至 v1.1.3 升级时会自动将 SQLite V10 或 V11 数据库迁移到 V12；更早格式不再支持，请使用新的数据库文件并重新配置。

### 后端

- 精简并统一参数校验、凭证状态和运行日志提示，保留诊断详情。

### 前端

- 修复 HTTP 访问下凭证标识计算、按凭证保存偏好和 API Key 复制的问题，并为剪贴板访问受限的环境提供兼容处理。
- 网络异常和响应格式错误显示简明提示，过滤代理错误页面并限制错误提示长度。
- 统一界面提示文案，准确展示上游凭证不可用状态。

## v1.2.2

### 升级须知

- 从 v1.0.0 至 v1.1.3 升级时会自动将 SQLite V10 或 V11 数据库迁移到 V12；更早格式不再支持，请使用新的数据库文件并重新配置。

### 后端

- 错误事件的错误类型优先采用上游 JSON 中的 `code`，未提供时回退到 `type`。
- 分析接口返回脱敏 API Key 标识，无需额外查询访问数据即可展示用量分布。

### 前端

- 全局刷新补齐当前标签页及顶部导航栏的数据依赖，分析内容不再等待 `access` 接口返回后才展示。
- 修复重复请求和旧会话响应干扰，稳定请求事件分页与导出的时间范围。
- 错误事件新增带数量统计的错误类型筛选，支持筛选空错误类型，并优化窄屏下的时间、类型与清除筛选布局。
- 导航栏及独立页面的全局刷新按钮新增加载旋转效果。

## v1.2.1

### 升级须知

- 从 v1.0.0 至 v1.1.3 升级时会自动将 SQLite V10 或 V11 数据库迁移到 V12；更早格式不再支持，请使用新的数据库文件并重新配置。

### 后端

- 记录所有主程序上报的用量事件，包括无 API Key 归属及 `generate=false` 的请求；无归属用量仅参与分析，不计入订阅费用。
- API Key 登录后的认证文件列表与限额查询遵循路由限制，且不混入配置型 AI 供应商凭证。

### 前端

- 新增标签页内刷新按钮，无需重新加载宿主页面即可更新当前数据。
- 优化 API Key 标识、已停用凭证、模型价格、错误事件和响应式导航的展示。

## v1.2.0

### 升级须知

- 从 v1.0.0 至 v1.1.3 升级时会自动将 SQLite V10 或 V11 数据库迁移到 V12；更早格式不再支持，请使用新的数据库文件并重新配置。

### 后端

- 订阅周期统一按秒存储和传输，移除周期类型；SQLite V10 和 V11 数据库会直接迁移到 V12。
- 以空绑定表示全部路由，移除 `system:all` 实体和特殊分支，并统一路由状态更新路径。
- API Key 的订阅、用量、并发和路由信息统一由 `access` 返回，移除冗余状态接口和已退役 Key 的管理视图。
- 统一认证额度响应、请求失败筛选和额度重置契约，简化分析成本与模型数据结构。

### 前端

- 路由选择以“全部路由”表示空绑定，支持直接清空选择，不再展示 `system:all`。
- 记住筛选条件、时间范围、分析维度、凭证开关和独立页面主题，并按登录凭证隔离。
- API Key 访问页改用统一访问数据，移除冗余运行状态展示。
- 修复切换用量分布维度时趋势图重复渲染的问题，并降低趋势图网格密度和对比度。

## v1.1.3

### 升级须知

- 从 v1.0.0 或 v1.0.1 升级时会自动迁移数据库，并将原有模型权限转换为路由规则。
- 从低于 v1.0.0 的版本升级时无法迁移原有数据，请使用新的数据库文件并重新配置。

### 后端

- 分析接口新增请求数、Token 分项、缓存率和成本趋势桶；24 小时及以内按小时、超过 24 小时按浏览器时区自然日聚合，并移除 RPM 和 TPM 计算。

### 前端

- 分析摘要卡新增独立配色、渐变背景和趋势折线图，并将 Token 与成本明细收紧为单行展示。
- 新增用量趋势组合图，以堆叠柱展示 Token 分项，以折线展示请求数、总成本和 Token 平均值。
- 优化移动端筛选和操作区布局，并修复 CPAMC 导航栏渐变边缘的内容泄露。

## v1.1.2

### 升级须知

- 从 v1.0.0 或 v1.0.1 升级时会自动迁移数据库，并将原有模型权限转换为路由规则。
- 从低于 v1.0.0 的版本升级时无法迁移原有数据，请使用新的数据库文件并重新配置。

### 后端

- 同步配置型上游凭证的安全指纹，使单个上游 API Key 无需预先调用即可用于精确路由。

### 前端

- 路由凭证列表直接显示配置型上游 API Key 的掩码信息，并区分“暂无凭证”和“暂无启用凭证”。
- 路由选择弹窗根据内容和可用空间自适应宽度，优化长名称、标签和“仅启用”开关的布局。

## v1.1.1

### 升级须知

- 从 v1.0.0 或 v1.0.1 升级时会自动迁移数据库，并将原有模型权限转换为路由规则。
- 从低于 v1.0.0 的版本升级时无法迁移原有数据，请使用新的数据库文件并重新配置。

### 后端

- 修复路由日志无法记录实际选中凭证的问题，并明确标记没有符合条件的可用凭证。
- 改进上游 API Key 凭证识别，以安全掩码显示凭证，不记录完整密钥。

### 前端

- 修复未使用过的配置型 AI 供应商不出现在路由凭证列表的问题。
- 路由凭证列表新增“仅启用”筛选，并区分认证文件与 AI 供应商，统一排序、图标和下拉框样式。

## v1.1.0

### 升级须知

- 从 v1.0.0 或 v1.0.1 升级时会自动迁移数据库，并将原有模型权限转换为路由规则。
- 从低于 v1.0.0 的版本升级时无法迁移原有数据，请使用新的数据库文件并重新配置。

### 后端

- 新增路由规则，可按 API Key 限制模型和上游凭证，并在符合规则的可用凭证间调度；路由决策会写入插件日志。模型无权访问时返回 HTTP 403，无可用凭证或配置无效时返回 HTTP 503。

### 前端

- 以路由规则管理取代可用模型管理，新增 API Key 路由选择，并将订阅计划和路由规则改为卡片布局。
- 优化 standalone、CPAMC 和 CPAMP 的桌面端与移动端显示。

## v1.0.1

### 前端

- 增加 API Key 复制按钮
- 修复可用模型选择器 bug

## v1.0.0

### 升级须知

- v1.0.0 不迁移旧版 JSON 或 SQLite 数据，升级后需要使用新的数据库并重新配置。
- 默认数据库路径改为 `plugins/cpa-key-billing-state-v1.db`。如果曾显式配置 `state_file`，请在升级前修改路径并保留旧文件作为备份。

### 后端

- 统一管理密钥与 API Key 登录的后端接口和数据结构。
- 重构数据库，分开保存请求事件、错误事件和插件运行日志。
- 移除 API Key 和模型的累计用量数据，分析结果改为根据请求事件实时计算。

### 前端

- 重新设计管理端和 API Key 自助页面，新增“分析”和“错误事件”。
- 新增请求数、Token、缓存率、成本和用量分布分析，支持按时间范围、API Key、模型和来源查看。
- 订阅计划、模型分组、模型定价和插件日志统一收纳到设置页。
- 重构刷新机制：首次进入页面立即加载；请求事件、错误事件和插件日志在滚动离开顶部后暂停自动刷新；分析数据不自动刷新。
- 完善独立页面、CPAMC 和 CPAMP 的主题适配及窄屏布局。
