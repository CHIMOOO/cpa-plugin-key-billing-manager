# CPA management origin; use the same CPA instance as the plugin UI.
# The management password goes in /etc/cpa-state-collector/management-key,
# not in this file or the command line.
CPA_URL=http://127.0.0.1:8317
